# first, run `getTodayRaceInfo.js" in crawler
# that gets the meta and runners info
# after that run `python3 generateSlowData.py` # slow data is in a csv
# get odds run `python3 getOdds.py` # fast data also in a csv


# then apply the model by running `predict.py ` 
# predict.py takes in the csvs saved in the folder
# encoding happens in predict